'use strict';

// node core modules

// 3rd party modules

// internal modules
const filesParser = require('./files-parser');
const foldersParser = require('./folders-parser');

module.exports = {
  filesParser,
  foldersParser,
};
